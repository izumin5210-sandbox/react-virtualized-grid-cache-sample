{
  "name": "react-virtualized-grid-cache-sample",
  "version": "0.1.0",
  "dependencies": {
    "react": "^15.5.3",
    "react-dom": "^15.5.3",
    "react-virtualized": "^9.7.2"
  },
  "devDependencies": {
    "flow-bin": "^0.43.1",
    "flow-typed": "^2.0.0",
    "react-scripts": "0.9.5"
  },
  "private": true,
  "scripts": {
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "preinstall": "flow-typed install",
    "pretest": "yarn run flow",
    "start": "react-scripts start",
    "test": "react-scripts test --env=jsdom"
  }
}
